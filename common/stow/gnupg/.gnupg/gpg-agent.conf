# gpg-agent — passphrase caching for pass(1).
# curses pinentry works everywhere a terminal does (incl. headless WSL);
# swap for a GUI pinentry in <platform>/stow if that ever matters.
pinentry-program /usr/bin/pinentry-curses
default-cache-ttl 28800
max-cache-ttl 86400
